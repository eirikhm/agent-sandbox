# AGENTS.md

Guidance for AI coding agents working in this repository.

## Project Overview

This repository is in an early stage. Update this section as the project's
purpose, language, and structure become established.

## Repository Structure

- `CONTRIBUTING.md` — How to contribute (currently a stub).
- `AGENTS.md` — This file; guidance for AI agents.

## Setup & Commands

No build, test, or run commands are defined yet. Add them here as the project
grows, for example:

```
# Install dependencies
# Run tests
# Build
# Lint
```

## Conventions

- Keep changes focused and small where possible (see `CONTRIBUTING.md`).
- Match the existing code style; document deviations.
- Write clear, descriptive commit messages.

## Code Style

- Use consistent indentation; match the surrounding file's existing style.
- Prefer clear, descriptive names over abbreviations.
- Keep functions small and focused on a single responsibility.
- Remove dead code and unused imports rather than commenting them out.
- Write comments to explain *why*, not *what*; let the code speak for itself.
- Format and lint code before committing once tooling is defined.
- Avoid introducing new dependencies without a clear justification.

## Workflow

1. Create a branch for your change.
2. Make and commit changes with a clear message.
3. Open a pull request describing what changed and why.

## Notes for Agents

- Prefer minimal, targeted edits over large rewrites.
- Update this file when project structure, commands, or conventions change.
- Verify changes with the relevant build/test commands once they exist.
